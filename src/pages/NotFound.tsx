import { Box } from '@mui/material';
import React from 'react';

function NotFound() {
  return (
    <main>
      <Box
        display="flex"
        justifyContent="center"
        alignItems="center"
        minHeight="100vh"
      >
        NOT FOUND PAGE
      </Box>
    </main>
  );
}

export default NotFound;
